# deps & build
node_modules
.next
*.log
.DS_Store
next-env.d.ts

# secrets — never commit
.env.local
data/.session-secret
data/.gmail-app-password
data/.gmail-user

# runtime data (live refresh churn + customer PII) — not version controlled.
# The running instance holds the real files on disk; git tracks code only.
data/*.json
data/.lead-seen.json
data/poll.db
data/contacts.db
data/contacts.db-*
data/_gmail-csv/
data/_gmail-uid*
data/_gmail-incoming.json
data/rep-inbox/
# StoneEagle PDFs + other pulled source docs (PII / business data)
sources/
tsconfig.tsbuildinfo
.claude/settings.local.json

# deal-jacket blanks (large binaries) + filled output
templates/
data/filled-packets/
